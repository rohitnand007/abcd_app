#content_header
  #breadcrumbs
    = bread_crumb_sub_topics_page
#inner-tab-menu
  %ul
    %li=link_to  "#{t("web-app-theme.list", :default => "List")}",current_user.is?("ET") ? teacher_contents_path : sub_topics_path
    %li.inner_tab_selected=link_to link_to "#{t("web-app-theme.new", :default => "New")}", new_sub_topic_path
#data
  %table{width: '100%'}
    %tr
      %td
        %div
          #application-list
            = render "form"