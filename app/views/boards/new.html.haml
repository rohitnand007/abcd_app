#content_header
  %h1#main_heading Boards
#inner-tab-menu
  %ul
    %li=link_to  "#{t("web-app-theme.list", :default => "List")}", boards_path
    %li.inner_tab_selected=link_to link_to "#{t("web-app-theme.new", :default => "New")}", new_board_path
#data
  %table{width: '100%'}
    %tr
      %td
        %div
          #application-list
            = render "form"


-#.block
  .secondary-navigation
    %ul.wat-cf
      %li.first= link_to "#{t("web-app-theme.list", :default => "List")}", boards_path
      %li.active= link_to "#{t("web-app-theme.new", :default => "New")}", new_board_path
  .content
    %h2.title
      = t("web-app-theme.new", :default => "New")
      board
    .inner
      = render "form"
-# content_for :sidebar, render(:partial => 'shared/sidebar')
