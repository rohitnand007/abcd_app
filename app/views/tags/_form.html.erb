<%= form_for(@tag) do |f| %>
  <% if @tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tag.errors.count, "error") %> prohibited this tag from being saved:</h2>

      <ul>
      <% @tag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <div class="field">
      <%= f.label :name %><br>
      <%= f.text_field :name, :readonly => 'readonly'%>
    </div>
    <br>
    <div class="field">
      <%= f.label :value %><br>
    <%= f.text_field :value %>
    </div>
    <br/>
    <% if @publisher_question_banks.present? %>
        <div class="field">
          <%= f.label :question_bank %><br>
          <%= collection_select :user, :question_bank_id, @publisher_question_banks, :id, :question_bank_name %>
        </div>
    <% end %>
    <br/>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
