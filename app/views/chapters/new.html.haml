#content_header
  #breadcrumbs
    = bread_crumb_chapters_page
#inner-tab-menu
  %ul
    %li=link_to  "#{t("web-app-theme.list", :default => "List")}",current_user.is?("ET") ? teacher_contents_path : chapters_path
    %li.inner_tab_selected=link_to link_to "#{t("web-app-theme.new", :default => "New")}", new_chapter_path
#data
  %table{width: '100%'}
    %tr
      %td
        %div
          #application-list
            = render "form"


-#.block
  .secondary-navigation
    %ul.wat-cf
      %li.first= link_to "#{t("web-app-theme.list", :default => "List")}", chapters_path
      %li.active= link_to "#{t("web-app-theme.new", :default => "New")}", new_chapter_path
  .content
    %h2.title
      = t("web-app-theme.new", :default => "New")
      chapter
    .inner
      = render "form"
-# content_for :sidebar, render(:partial => 'shared/sidebar')
