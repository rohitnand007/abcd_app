#content_header
  %h1#maend_heading assessment_inclasses
#inner-tab-menu
  %ul
    %li= link_to "#{t("web-app-theme.list", :default => "List")}",assessment_inclasses_path
    %li= link_to "#{t("web-app-theme.new", :default => "New")}", new_assessment_inclass_path
    %li= link_to "#{t("web-app-theme.edit", :default => "Edit")}", edit_assessment_inclass_path(@assessment_inclass)
    %li.inner_tab_selected= link_to "#{t("web-app-theme.show", :default => "Show")}", assessment_inclass_path(@assessment_inclass)
#data
  #application-list
    .heading= @assessment_inclass.name
    %table.details{width: '700px',align: 'center'}
      %tbody
        %tr.odd.left
          %td= t("activerecord.attributes.assessment_inclass.rb.name", :default => t("activerecord.labels.name", :default => "Name"))
          %td.right= @assessment_inclass.name
        %tr.even.left
          %td
          - if current_user.role.name.eql?('Support Team')
            %td.right= link_to "Download", est_download_content_path(@assessment_inclass) ,:title=>'Download Attachement',:class=>'tipTipRight'
          - else
            %td.right= link_to "Download", download_zip_content_path(@assessment_inclass) ,:title=>'Download Attachement',:class=>'tipTipRight'

        %tr.tr-blank
        %tr
          %td{:colspan=>'2'}
            = link_to 'Edit',edit_assessment_inclass_path(@assessment_inclass), :class => "button icon edit"
            = link_to 'Delete',assessment_inclass_path(@assessment_inclass), :method => "delete",:class => "button danger icon remove", :confirm => "Are you sure?"